<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="http://0.0.0.0:4000/feed.xml" rel="self" type="application/atom+xml" /><link href="http://0.0.0.0:4000/" rel="alternate" type="text/html" /><updated>2025-09-23T14:14:42+00:00</updated><id>http://0.0.0.0:4000/feed.xml</id><entry><title type="html">Starting a blog</title><link href="http://0.0.0.0:4000/blog/2025/09/23/setting-this-up" rel="alternate" type="text/html" title="Starting a blog" /><published>2025-09-23T00:00:00+00:00</published><updated>2025-09-23T00:00:00+00:00</updated><id>http://0.0.0.0:4000/blog/2025/09/23/setting-this-up</id><content type="html" xml:base="http://0.0.0.0:4000/blog/2025/09/23/setting-this-up"><![CDATA[<h1 id="starting-a-blog">Starting a blog</h1>

<p><em>In this, more than ever, fast changing times in tech I decided to set up a blog. I am not sure what will come out of this other than me writing down stuff and trying to explain what I learn.</em></p>

<h2 id="tldr">TL;DR</h2>

<ul>
  <li>Use GitHub Pages + Jekyll for a simple, free blog</li>
  <li>Create posts under <code class="language-plaintext highlighter-rouge">_posts/</code> with <code class="language-plaintext highlighter-rouge">YYYY-MM-DD-title.md</code></li>
  <li>Add a blog index page and optional <code class="language-plaintext highlighter-rouge">/blog/</code> permalinks</li>
  <li>Run locally with Docker for faster iteration</li>
</ul>

<h2 id="contents">Contents</h2>

<h2 id="introduction">Introduction</h2>

<p>After looking around seems like github pages might be the best resource, like its simplicity and I dont like this kind of monetized-paywalls like medium and so on.</p>

<p>The list of resources I used so far is:</p>

<ul>
  <li><a href="https://medium.com/cardano/creating-an-engineering-blog-with-github-pages-4e9bf423c1d0">Creating an Engineering Blog with GitHub Pages</a>.</li>
  <li><a href="https://jmcglone.com/guides/github-pages/">Creating and Hosting a Personal Site on GitHub</a>.</li>
  <li><a href="https://www.smashingmagazine.com/2014/08/build-blog-jekyll-github-pages/">Build A Blog With Jekyll And Github Pages</a></li>
</ul>

<p>This assumes you are qutie familiar already with tools like Git, Github and can understand some basic web development cocnepts using HTML/CSS/Javascript.</p>

<h2 id="git--github">Git &amp; Github</h2>

<p>Git is an OSS tool to keep track of the state of a file (VCS, Version Control System) and understand all of its transformations over time, making it easy to jump back and forth between those (think of those university projects that kept getting created <code class="language-plaintext highlighter-rouge">project.doc</code>, then <code class="language-plaintext highlighter-rouge">project_v2.doc</code>, <code class="language-plaintext highlighter-rouge">project_v2_final.doc</code>, but you dont need to create multiple files and can jump back and forth somewhat easily).
It is a very powerful tool, it can really do a lot of things and I probably only use around 5% of its commands but you can find more information in <a href="https://git-scm.com/">Git’s page</a>.</p>

<p>Github is a service that (among other things) offers you to host those projects online, so makes it easy to collaborate with other people online (happens a lot in software), there are similar platforms also such as Gitlab.</p>

<p>Although all of those Git + whatever might be confusing, the distinction between the actual technology(Git) and the platforms (Github, Gitlab) offering its services its important, and you need to beceome somewhat familiar with it over your career.</p>

<h2 id="github-pages">Github pages</h2>

<p>Github pages are a way to create a free website (or subdomain) in their page, its quite tech oriented (given that Github is a tech company) and easy to set up. My personal thoughts on why its great:</p>

<ul>
  <li>Free.</li>
  <li>You will get familiar with git.</li>
  <li>Simple</li>
</ul>

<h2 id="creating-the-blog">Creating the blog</h2>

<p>The first step is quite easy, assuming you have a github account just create a new repository with <code class="language-plaintext highlighter-rouge">yourusername.github.io</code>, visibility public and I recommend marking the option add README.</p>

<p>After creating it update a file <code class="language-plaintext highlighter-rouge">index.html</code> on the root folder of the repository (hint, if you don’t know git at this point would be great if you pause and learn how to pull a repository locally, add files and upload(push) them, or the least recommended option is to use the UI).</p>

<p>The file could be as simple as:</p>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">&lt;!DOCTYPE html&gt;</span>
<span class="nt">&lt;html&gt;</span>
  <span class="nt">&lt;head&gt;</span>Hello World<span class="nt">&lt;/head&gt;</span>
  <span class="nt">&lt;h1&gt;</span>Welcome<span class="nt">&lt;/h1&gt;</span>
<span class="nt">&lt;/html&gt;</span>
</code></pre></div></div>

<p>Now going to <code class="language-plaintext highlighter-rouge">yourusername.github.io</code> should show a very simple ugly page welcoming you.
Here is where you can get some insipration going on and feel free to use HTML, CSS to make it look fancier (see <a href="#additional-resources">Additional resources</a>), we will just be covering some very basics below.</p>

<h2 id="getting-fancy">Getting fancy</h2>

<p>We will be adding some Cascading Style Sheet (CSS) which can be understood as a way to define styles for HTML documents, modifying fonts, size, color of HTML elements.</p>

<p>In order to avoid having all of our files lying around we will be creating a specific folder for storing our css files and create our first file under <code class="language-plaintext highlighter-rouge">css/main.css</code>, which will look like:</p>

<div class="language-css highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">body</span> <span class="p">{</span>
	<span class="nl">background-color</span><span class="p">:</span> <span class="s1">"#F0F8FF"</span><span class="p">;</span>
<span class="p">}</span>

<span class="nt">h1</span> <span class="p">{</span>
	<span class="nl">font-family</span><span class="p">:</span> <span class="s2">'Comic Sans MS'</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Now after having this file uploaded (pushed) to your repository, if you wait a little and refresh your page things should look slightly different. You can really get some cool stuff done with just HTML/CSS so I’d recommend you playing a little bit around before if you are not familiar yet.</p>

<h2 id="jekyll">Jekyll</h2>

<p>Jekyll is a new tech for me, so we will be learning together. 
It seems to be a way to go from plainetxt to some cool websites and blogs, so we are just going to set up some structure in our repository (folder where our website conents are) and it will automatically pick it up and save us some time.
It fits very well with Github pages simplicty where we can just upload the files directly and no need of a database.</p>

<p>One of the advantages is using templates (layouts) so you can save some time avoiding some repetitive tasks, the folder structure is defined <a href="https://jekyllrb.com/docs/structure/">here</a>, I will kinda speed run this but the link to <code class="language-plaintext highlighter-rouge">Creating and Hosting a Personal Site on GitHub</code> in the <a href="#introduction">introduction</a> explains it with more details.</p>

<p>An important file is the <code class="language-plaintext highlighter-rouge">._config.yml</code>, I recommend checking the official docs <a href="https://jekyllrb.com/docs/configuration/">here</a> and also its <a href="https://jekyllrb.com/docs/configuration/default/">defaults values</a>, as you explore the tool and feel you are missing something go and explore if its already implemented.</p>

<div class="language-yml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">lsi</span><span class="pi">:</span> <span class="kc">true</span>
<span class="na">safe</span><span class="pi">:</span> <span class="kc">true</span>
<span class="na">gist</span><span class="pi">:</span>
  <span class="na">noscript</span><span class="pi">:</span> <span class="kc">false</span>
<span class="na">kramdown</span><span class="pi">:</span>
  <span class="na">math_engine</span><span class="pi">:</span> <span class="s">mathjax</span>
  <span class="na">syntax_highlighter</span><span class="pi">:</span> <span class="s">rouge</span>

</code></pre></div></div>

<p>The next step is creating the <code class="language-plaintext highlighter-rouge">_layouts</code> folder, where we will create a <code class="language-plaintext highlighter-rouge">default.html</code> which will be added on every post, so it makes sense for us to define a header and a footer (HTML tags).</p>

<p>The cool thing of the layouts is you can reference then them anywhere from another html file, so we can do the following on our <code class="language-plaintext highlighter-rouge">index.html</code> file:</p>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code>---
layout: default
title: This is the first attempt
---
<span class="nt">&lt;div&gt;</span>
  <span class="nt">&lt;h1&gt;</span>Welcome<span class="nt">&lt;/h1&gt;</span>
<span class="nt">&lt;/div&gt;</span>
</code></pre></div></div>
<p>Notice at the top of the file? Yhose are called <code class="language-plaintext highlighter-rouge">Front-matter</code> in Jekyll and this means every file starting with those characters will be processed by the framework, and basically we are just calling it to use our <code class="language-plaintext highlighter-rouge">layouts/_default</code> to craete a new HTML page where the content will be replaced in the ``.</p>

<h2 id="the-blog-itself">The blog itself</h2>

<p>Jekyll has a lot out of the box features to create a blog and allows for customization but the tutorials that we followed will cover the basics to creat a post, a page to index our posts, creating permalinks for our posts and finally RRS feed.</p>

<p>To creat a post layout we go back to <code class="language-plaintext highlighter-rouge">layouts</code> and create a <code class="language-plaintext highlighter-rouge">post.html</code> file, something as simple as:</p>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code>---
layout: default
---
<span class="nt">&lt;h1&gt;</span> Starting a blog <span class="nt">&lt;/h1&gt;</span>
<span class="nt">&lt;p</span> <span class="na">class=</span><span class="s">"meta"</span><span class="nt">&gt;</span>Written the 23 Sep 2025<span class="nt">&lt;/p&gt;</span>

<span class="nt">&lt;div</span> <span class="na">class=</span><span class="s">"post"</span><span class="nt">&gt;</span>
	
<span class="nt">&lt;/div&gt;</span>
</code></pre></div></div>

<p>We need to create a directory posts where we will store our posts <code class="language-plaintext highlighter-rouge">_posts/</code> and now its important we are very strict with the name of the files under and follow the convention as <code class="language-plaintext highlighter-rouge">YYYY-MM-DD-title-of-post.md</code>, so we can do something like <code class="language-plaintext highlighter-rouge">2025-09-21-first-post.md</code>with the content:</p>

<div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="na">layout</span><span class="pi">:</span> <span class="s">post</span>
<span class="na">title</span><span class="pi">:</span> <span class="s2">"</span><span class="s">We</span><span class="nv"> </span><span class="s">try</span><span class="nv"> </span><span class="s">our</span><span class="nv"> </span><span class="s">first</span><span class="nv"> </span><span class="s">post"</span>
<span class="na">date</span><span class="pi">:</span> <span class="s">2025-21-09</span>
<span class="nn">---</span>
This is where we try our first post following mostly jmcglone's tutorial
</code></pre></div></div>

<p>Notice some of the elements between <code class="language-plaintext highlighter-rouge">---</code>, like title or date, those should be familiar with our <code class="language-plaintext highlighter-rouge">post.html</code> template.
If everything went fine (e.g I missed up and forgot the <code class="language-plaintext highlighter-rouge">_</code> before the <code class="language-plaintext highlighter-rouge">posts</code> directory), you should be able to see your post in <code class="language-plaintext highlighter-rouge">YYYY-MM-DD-title-of-my-post.html</code>, great! But having to remember all of this is quite annoying so let’s first build a page where we canlist all of our post and just click to access them.</p>

<p>In order to do so, we need an additional directory named <code class="language-plaintext highlighter-rouge">blog</code>, where we are adding the following html file (note that this is not “pure” (whatever this means) HTML it self, it contains some templating), in this case we are going to loop for all the files under our post directory and list them in the file <code class="language-plaintext highlighter-rouge">index.html</code> (under blog dir):</p>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code>---
layout: default
title: AlejandroUPC's Blog
---
	<span class="nt">&lt;h1&gt;</span>{{ page.title }}<span class="nt">&lt;/h1&gt;</span>
	<span class="nt">&lt;ul</span> <span class="na">class=</span><span class="s">"posts"</span><span class="nt">&gt;</span>

	  {% for post in site.posts %}
	    <span class="nt">&lt;li&gt;&lt;span&gt;</span>{{ post.date | date_to_string }}<span class="nt">&lt;/span&gt;</span>
              <span class="nt">&lt;a</span> <span class="na">href=</span><span class="s">"{{ post.url }}"</span> <span class="na">title=</span><span class="s">"{{ post.title }}"</span><span class="nt">&gt;</span>{{ post.title }}<span class="nt">&lt;/a&gt;&lt;/li&gt;</span>
	  {% endfor %}
	<span class="nt">&lt;/ul&gt;</span>
</code></pre></div></div>

<p>Now if you just access your page and add <code class="language-plaintext highlighter-rouge">/blog</code> at the end you should see at least one post that we just created earlier!</p>

<p>You probably have seen the post links do not contain the <code class="language-plaintext highlighter-rouge">/blog</code> directory under the URL, this might or not be relevant to you but it might make sense to change the way the URLs are built under posts to include it, you can go back to your <code class="language-plaintext highlighter-rouge">_config.yml</code> file and add this to modify how the permalinks are created:</p>

<div class="language-yml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">permalink</span><span class="pi">:</span> <span class="s">/blog/:year/:month/:day/:title</span>
</code></pre></div></div>

<p>Now you should be able to access your posts as <code class="language-plaintext highlighter-rouge">http://username.github.io/blog/YYYY/MM/DD/name-of-your-post.</code>.</p>

<p>Finally one of the last steps could be to set up a <a href="https://en.wikipedia.org/wiki/RSS">RSS</a> feed for your blog, this is quite easy and you simply create a file named <code class="language-plaintext highlighter-rouge">atom.xml</code> under the <code class="language-plaintext highlighter-rouge">blog/</code> dir.</p>

<div class="language-xml highlighter-rouge"><div class="highlight"><pre class="highlight"><code>---
layout: feed
---
<span class="cp">&lt;?xml version="1.0" encoding="utf-8"?&gt;</span>
<span class="nt">&lt;feed</span> <span class="na">xmlns=</span><span class="s">"http://www.w3.org/2005/Atom"</span><span class="nt">&gt;</span>

	<span class="nt">&lt;title&gt;</span>Hank Quinlan's Blog<span class="nt">&lt;/title&gt;</span>
	<span class="nt">&lt;link</span> <span class="na">href=</span><span class="s">"http://alejandroupc.github.io/blog/atom.xml"</span> <span class="na">rel=</span><span class="s">"self"</span><span class="nt">/&gt;</span>
	<span class="nt">&lt;link</span> <span class="na">href=</span><span class="s">"http://alejandroupc.github.io/blog"</span><span class="nt">/&gt;</span>
	<span class="nt">&lt;updated&gt;</span>{{ site.time | date_to_xmlschema }}<span class="nt">&lt;/updated&gt;</span>
	<span class="nt">&lt;id&gt;</span>http://alejandroupc.github.io/blog<span class="nt">&lt;/id&gt;</span>
	<span class="nt">&lt;author&gt;</span>
		<span class="nt">&lt;name&gt;</span>AlejandroUPC<span class="nt">&lt;/name&gt;</span>
		<span class="nt">&lt;email&gt;</span>amartinezotal@gmail.com<span class="nt">&lt;/email&gt;</span>
	<span class="nt">&lt;/author&gt;</span>

	{% for post in site.posts %}
		<span class="nt">&lt;entry&gt;</span>
			<span class="nt">&lt;title&gt;</span>{{ post.title }}<span class="nt">&lt;/title&gt;</span>
			<span class="nt">&lt;link</span> <span class="na">href=</span><span class="s">"http://alejandroupc.github.io{{ post.url }}"</span><span class="nt">/&gt;</span>
			<span class="nt">&lt;updated&gt;</span>{{ post.date | date_to_xmlschema }}<span class="nt">&lt;/updated&gt;</span>
			<span class="nt">&lt;id&gt;</span>http://alejandroupc.github.io{{ post.id }}<span class="nt">&lt;/id&gt;</span>
			<span class="nt">&lt;content</span> <span class="na">type=</span><span class="s">"html"</span><span class="nt">&gt;</span>{{ post.content | xml_escape }}<span class="nt">&lt;/content&gt;</span>
		<span class="nt">&lt;/entry&gt;</span>
	{% endfor %}

<span class="nt">&lt;/feed&gt;</span>
</code></pre></div></div>

<p>So that’s it pretty much, now you can add posts to your blog, add it to RSS and share it with whoever (As Jmcglone recommends do not ofroget to create the <code class="language-plaintext highlighter-rouge">about/index.html</code> and <code class="language-plaintext highlighter-rouge">cv/index.html</code> pages for  the links we added before in our header/footer)</p>

<h2 id="setting-up-a-local-environment">Setting up a local environment</h2>

<p>All of this setup is cool, but in order to see the results we need to commit our code, github runs some steps in the background (a pipeline) and then publishes it to “the internet”, there might be a few things that can be improved here:</p>
<ol>
  <li>The feedback loop feels slow, maybe we could see what it looks like before it reaches the internet and review for typos, format issues, etc …</li>
  <li>We might break something from Jekyll and the pipeline will fail, changes will not show, we will have to keep committing until fixed and this can be tedious.</li>
  <li>We might want to test also stuff before uploading it.</li>
</ol>

<p>This where Docker comes into play, which is a tool that allows you to containerize software and run it into isolated environments. Basically if you are not very familiar what we are going to try to do is follow the same steps that Github does in their own pipeline and be able to see the website in our computer before pushing the changes.</p>

<p>From here we assume you are familiar with Docker enough to deploy and run containers locally,
you can set up the following Dockerfile:</p>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="s"> ruby:3.1-slim</span>
<span class="k">ENV</span><span class="s"> LANG=C.UTF-8 \</span>
    LC_ALL=C.UTF-8 \
    DEBIAN_FRONTEND=noninteractive
<span class="k">RUN </span>apt-get update <span class="o">&amp;&amp;</span> apt-get <span class="nb">install</span> <span class="nt">-y</span> <span class="se">\
</span>    build-essential <span class="se">\
</span>    git <span class="se">\
</span>    curl <span class="se">\
</span>    <span class="o">&amp;&amp;</span> <span class="nb">rm</span> <span class="nt">-rf</span> /var/lib/apt/lists/<span class="k">*</span>
<span class="k">WORKDIR</span><span class="s"> /usr/src/app</span>
<span class="k">COPY</span><span class="s"> Gemfile* ./</span>
<span class="k">RUN </span>gem <span class="nb">install </span>bundler <span class="o">&amp;&amp;</span> bundle <span class="nb">install</span>
<span class="k">COPY</span><span class="s"> . .</span>
<span class="k">EXPOSE</span><span class="s"> 4000</span>
<span class="k">CMD</span><span class="s"> ["bundle", "exec", "jekyll", "serve", "--host", "0.0.0.0", "--watch"]</span>
</code></pre></div></div>

<p>We will need to create a <code class="language-plaintext highlighter-rouge">Gemfile</code> (what is a <a href="https://jekyllrb.com/docs/ruby-101/">Gemfile</a>) with this content:</p>

<div class="language-ruby highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">source</span> <span class="s2">"https://rubygems.org"</span>

<span class="n">gem</span> <span class="s2">"jekyll"</span>
<span class="n">group</span> <span class="ss">:jekyll_plugins</span> <span class="k">do</span>
  <span class="n">gem</span> <span class="s2">"jekyll-feed"</span>
  <span class="n">gem</span> <span class="s2">"jekyll-seo-tag"</span>
<span class="k">end</span>
</code></pre></div></div>

<p>And finally run the command</p>

<div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker run <span class="nt">-p</span> 4000:4000 <span class="nt">-v</span> <span class="s2">"</span><span class="nv">$PWD</span><span class="s2">"</span>:/usr/src/app jekyll-site
</code></pre></div></div>

<p>Now you should be able to go to <code class="language-plaintext highlighter-rouge">http://localhost:4000/blog</code> and you should be able to see the same as will be later on your site in github.</p>

<h2 id="closing">Closing</h2>

<p>This is the process I used to set up this blog, which leaves it in a quite basic state. If any improvements there will probably in a future entry. The process (if familiar with the tech stack involved) is quite straight forward and the authors of the articles I used (and linked) did an amazing job.</p>

<h3 id="additional-resources">Additional resources</h3>

<ul>
  <li><a href="">Git documentation</a></li>
  <li><a href="https://developer.mozilla.org/es/docs/Web/HTML">HTML</a></li>
  <li><a href="https://developer.mozilla.org/es/docs/Web/CSS">CSS Mozilla Docs</a></li>
  <li><a href="https://jekyllrb.com/">Jekyll</a></li>
  <li><a href="https://www.docker.com/">Docker</a></li>
  <li><a href="https://jogendra.dev/dockerize-your-jekyll-site-for-local-development">Dockerize your Jekyll site</a></li>
</ul>]]></content><author><name></name></author><category term="jekyll" /><category term="github" /><category term="git" /><category term="docker" /><category term="html" /><category term="css" /><summary type="html"><![CDATA[Starting a blog]]></summary></entry></feed>